# Page Activation Surfaces And Route Subscription Overrides

Alva will treat page activation as five surfaces: declarative `collections:` and `signals:` for normal projection activation, top-level `route_subscriptions:` for per-projection Topic wiring, declarative `page_state:` for page-owned derived assigns, declarative `page_events:` for page-owned request/reply control flow, including branchy command dispatch replies and navigation side effects such as `push_patch`, and raw Phoenix PubSub escape hatches for flows outside the Alva projection model. Projection activation surfaces may derive config from Page Scope, including auth or permission checks, but must not perform control flow; `page_state:` is the declarative surface for page-owned state derived from Route Params and other Page Scope inputs, while `page_events:` is the separate declarative surface for page-owned control flow that still needs first-class library ownership instead of raw `handle_event/3` escape hatches. Default route subscription inference is retained only when the topic set is deterministic, otherwise page activation fails loudly and requires explicit `route_subscriptions:` for the affected projection. Projection-keyed `route_subscriptions:` entries only override already-active projections and must fail loudly when they reference a projection that is not active. Because active projections are declaration-known, that target validation should fail at compile-time rather than waiting for activation. Declarative projection activation uses one shared page-level projection namespace across `collections:`, `signals:`, and `route_subscriptions:` targets, so a page may not activate the same Declaration Key as both a Collection and a Signal. Declarative activation uniqueness is consistent across the whole surface: duplicate `collections:`, duplicate `signals:`, duplicate `route_subscriptions:` entries for the same projection, duplicate `page_events:` entries for the same browser event name, duplicate `page_state:` declarations, and cross-kind Collection/Signal activation collisions for the same projection key are all declaration conflicts and should fail at compile-time. Overrides are partial rather than all-or-nothing: projections not listed under `route_subscriptions:` continue through deterministic inference and fail loudly if they are not inferable. An explicit empty topic list is authoritative and suppresses inference for that projection, allowing a page to keep the projection active without realtime topic wiring. Callback-based `route_subscriptions:` follow the same rule: returning `[]` is an authoritative dynamic opt-out for the current Page Scope, not a fallback to inference. `nil` is invalid and must fail loudly rather than acting like an implicit opt-out. Callback-based `route_subscriptions:` are also route-lifecycle aware: when Page Scope changes through route lifecycle updates, Alva must re-evaluate those callbacks so realtime Topic scope tracks current Route Params and other page-owned scope. Pages with callback-based `route_subscriptions:` therefore participate in route lifecycle handling even when they have no `reload_on: :route_change` Collections. Pages with declarative `page_state:` likewise participate in route lifecycle handling so derived assigns stay in sync with current Route Params after `push_patch` or other route changes. Explicit `route_subscriptions:` wiring is authoritative and page-owned: Alva validates only the target projection and the topic-shape contract, not whether the supplied Topics can be re-derived from publication templates. Valid Topic lists may still be normalized for transport hygiene, including duplicate removal before subscribe. Pages that need subscriptions outside the projection model should use raw Phoenix PubSub directly. `route_subscriptions:` only owns Topic wiring; it does not replace a Collection's required source or a projection's `on:` PubSub occurrence trigger. For API shape, `route_subscriptions:` uses a top-level list of tuples keyed directly by projection Declaration Key for both Collections and Signals, so projection activation and infra topic wiring stay separate without introducing a second per-kind nesting layer. `page_state:` uses a single local callback atom keyed at the page level; that callback returns a map of atom-keyed assigns and may derive values from `Alva.LiveView.route_params/1` or other page-owned scope, but should not perform control flow. `page_events:` likewise uses a top-level list of `{event_name, callback}` tuples keyed by browser-facing event name; callbacks are local page callbacks that return `{:reply, map, socket}`, may dispatch Alva events internally, and must not shadow host app dispatcher event names. When multiple active projections resolve to the same concrete Topic, the socket subscribes only once at the transport layer; projection semantics remain independent, so one incoming occurrence may still update multiple Collections and/or fire multiple Signals. Declarative route lifecycle handling first captures the latest Route Params and current Page Scope for active projection wiring, then diffs projection-owned Topic scope, runs any route-change Collection refresh behavior, and re-syncs `page_state:` before rendering. If callback-derived Topic recompute fails during that route lifecycle step, Alva must fail loudly rather than preserving the previous projection-owned Topic set. Declarative activation is therefore allowlisted: only keyword-form `use Alva.LiveView` declarations with `collections:`, `signals:`, `route_subscriptions:`, `page_state:`, and `page_events:` are valid public page-activation syntax. Legacy tuple forms are not part of the supported surface and should fail at compile-time. The allowlist also applies inside projection activation options: Collection activation uses keyed entries only, with opts reserved for route-owned concerns such as `source_input:` and `reload_on:`. Signal activation uses atom Declaration Keys such as `signals: [:order_fulfilled]`; the client-facing signal name stays in the resource declaration as `name: "order.fulfilled"` and is not repeated in page activation. Signal activation has no opts until a concrete route-owned capability justifies them. Topic wiring for both stays top-level under `route_subscriptions:`. Any other declarative activation key or shape, including legacy `subscriptions:` and declarative `streams:`, is invalid and should raise immediately. Collection activation opts such as nested `params:` / `subscriptions:` remain invalid. Pages that still need non-projection realtime wiring must use raw Phoenix PubSub until they are modeled as Collections or Signals. Activation validation therefore happens in two strict layers: public-surface allowlist validation first, then deterministic inference validation only for still-valid declarative projections. Allowlist failures are compile-time errors because the invalid shape is knowable from the page declaration itself. Deterministic inference failures are activation-time errors because projection/resource wiring and publication matching are resolved there. Deterministic inference means the final Topic set is static, finite, and derivable from projection/resource declarations alone; if Topic wiring depends on runtime Page Scope, callback execution, or ambiguous publication expansion, inference is invalid and explicit `route_subscriptions:` is required. A projection trigger that matches more than one publication is treated as ambiguous even if those publications would expand to a static union of Topics. A single publication may still expand to multiple Topics and remain deterministic as long as that expansion is static and fully derivable from the declaration itself. Publication topic callbacks are never eligible for deterministic inference, even if they would happen to return static Topics for one page instance.
